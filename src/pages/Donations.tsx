import { MainLayout } from "@/components/layout/MainLayout";

const Donations = () => {
  return (
    <MainLayout title="Donations" subtitle="Record and manage donations">
      <div className="glass-card p-8 text-center">
        <h2 className="text-xl font-semibold text-foreground mb-2">Donation Management</h2>
        <p className="text-muted-foreground">Coming soon - POS-style donation entry</p>
      </div>
    </MainLayout>
  );
};

export default Donations;
